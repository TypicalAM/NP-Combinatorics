package input

import (
	g "github.com/TypicalAM/NP-Combinatorics/src/graph"
)

// Load the data from a file (filepath) and convert it to a proper graph
func LoadData(filepath string) (g.Graph, error) { return g.Graph{}, nil }
